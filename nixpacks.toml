[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install --prefer-offline",
  "cd client && npm install --omit=none"
]
cacheDirectories = ["node_modules", ".npm"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "npm start"
