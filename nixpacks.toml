[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "cd client && npm install --omit=none"
]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "npm start"
